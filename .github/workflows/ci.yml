name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    name: build · vet · race-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: gofmt
        run: |
          out="$(gofmt -l .)"
          if [ -n "$out" ]; then
            echo "::error::files not gofmt'd:"
            echo "$out"
            exit 1
          fi

      - name: go vet
        run: go vet ./...

      - name: go test (-race)
        run: go test -race -count=1 ./...

      - name: build binary
        run: go build -trimpath -ldflags="-s -w" -o /tmp/o3ui ./cmd/openvpn3ui-tui

      # Probes the embedded desklet is wired in correctly — the install
      # command writes 4 of its files to the destination dir.
      - name: smoke desklet install
        run: |
          DEST=/tmp/desklet
          mkdir -p "$DEST"
          HOME=/tmp /tmp/o3ui desklet install
          test -f /tmp/.local/share/cinnamon/desklets/o3ui@esivres/desklet.js
          test -f /tmp/.local/share/cinnamon/desklets/o3ui@esivres/stylesheet.css
          test -f /tmp/.local/share/cinnamon/desklets/o3ui@esivres/metadata.json

  lint:
    name: golangci-lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      # Pin the action and golangci-lint to a release that knows about
      # the v2 config schema *and* is built with a Go ≥ go.mod's
      # directive. modernc.org/sqlite drags us up to 1.25; v2.1.6 was
      # built with 1.24 and refuses to load the config. v2.12.x is the
      # latest line that's still compatible with our setup.
      - uses: golangci/golangci-lint-action@v8
        with:
          version: v2.12.2
          args: --timeout=5m
